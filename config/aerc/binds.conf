# Portable navigation only. Account-specific message actions are deliberately
# omitted from the public snapshot.

[messages]
j = :next<Enter>
k = :prev<Enter>
g = :select 0<Enter>
G = :select -1<Enter>
<Enter> = :view<Enter>

[view]
j = :next<Enter>
k = :prev<Enter>
q = :close<Enter>
